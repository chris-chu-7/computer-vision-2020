b. The voting schemes that I have used is a few smaler bins, since they are way easier to compute this way.

c. To determine the peaks of the Hough array, I performed the following: 
I compared the hough accumulator array to the immages and color whether certain pixels overlap or not. 